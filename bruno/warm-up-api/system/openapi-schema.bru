meta {
  name: OpenAPI Schema
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/openapi.json
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("schema has paths", function() {
    expect(res.getBody()).to.have.property("paths");
  });
}
