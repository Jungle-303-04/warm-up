meta {
  name: Root Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("root response is alive", function() {
    expect(res.getBody()).to.have.property("hello", "world");
  });
}
